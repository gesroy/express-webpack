/*export default class ColorToggle {
  counter = 0
  colors = ['skyblue', 'hotpink', 'seagreen']
  constructor(el) {
    el.addEventListener('click', () => {
      el.style.color = this.colors[this.counter]
      this.counter =
        this.counter === this.colors.length - 1 ? 0 : this.counter + 1
    })
  }
}*/
